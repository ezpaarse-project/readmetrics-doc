<template><h2 id="run-the-server" tabindex="-1"><a class="header-anchor" href="#run-the-server" aria-hidden="true">#</a> Run the server</h2>
<p>ReadMETRICS launches from the command line.</p>
<p>Use the following commands from the installation directory to start and stop the server :</p>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code>./launch.sh  <span class="token comment"># start ReadMETRICS</span>
./stop.sh    <span class="token comment"># stop ReadMETRICS</span>
</code></pre><div class="line-numbers"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div></template>
